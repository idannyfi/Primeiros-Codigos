Autor Dannyfi Data:17/07/2024

print('Pode ser triângulo ou não ')
r1 = float(input('Digite o tamanho do segmento 1: '))
r2 = float(input('Digite o tamanho do segmento 2: '))
r3 = float(input('Digite o tamanho do segmento 3: '))

if r1 > r2+r3 or r2 > r1 + r3 or r3 > r1 + r2:
    print('Os segmentos não podem formam um triângulo! ')

else:
    print('Os segmentos podem formar um triângulo! ')
